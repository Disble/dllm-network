/**
 * App composes the scaffold shell for the dashboard slices that follow.
 */
export function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">Passive-only scaffold</p>
        <h1>Ollama Telemetry</h1>
        <p className="body-copy">
          Confirmed telemetry, inferred activity, and collector streaming arrive in later work units.
        </p>
      </section>
    </main>
  );
}
