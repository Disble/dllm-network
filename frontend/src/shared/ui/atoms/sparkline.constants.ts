/**
 * SPARKLINE_DEFAULT_WIDTH is the default SVG width for a Sparkline.
 */
export const SPARKLINE_DEFAULT_WIDTH = 80;

/**
 * SPARKLINE_DEFAULT_HEIGHT is the default SVG height for a Sparkline.
 */
export const SPARKLINE_DEFAULT_HEIGHT = 24;

/**
 * SPARKLINE_PADDING is the inner padding in pixels applied on each side of the sparkline path.
 */
export const SPARKLINE_PADDING = 2;
